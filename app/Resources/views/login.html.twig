{% if app.user %}
    <a href="{{ path("fos_user_security_logout") }}" id="Deconnexion"><i id="connexion" class="fa fa-user fa-2x" aria-hidden="true"></i></a>
{% else %}
    <a href="{{ path("fos_user_security_login") }}" id="Connexion"><i id="connexion" class="fa fa-user fa-2x" aria-hidden="true"></i></a>
{% endif %}