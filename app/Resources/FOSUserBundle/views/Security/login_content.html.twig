{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div class="text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}


<div class="formulaire-triangle"></div>
<h2 class="formulaire-header">{% block formTitle %}Connexion{% endblock %}</h2>
<form class="text-center formulaire-container" action="{{ path("fos_user_security_check") }}" method="post">
{% if csrf_token %}
    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
{% endif %}
    <div class="form-group">
        <p><label for="username">{{ 'security.login.username'|trans }}</label></p>
        <p><input  class="text-center" placeholder="Nom d'utilisateur" type="text" id="username" name="_username" value="{{ last_username }}" required="required" /></p>
    </div>

    <div class="form-group">
        <p><label for="password">{{ 'security.login.password'|trans }}</label></p>
        <p><input class="text-center" type="password" placeholder="Mot de passe" id="password" name="_password" required="required" /></p>
    </div>
    <div class="checkbox">
        <p><input type="checkbox" id="remember_me" name="_remember_me" value="on" /></p>
        <p><label for="remember_me">{{ 'security.login.remember_me'|trans }}</label></p>
    </div>

    <input class="btn" type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" />

    <div><a href={{ path("fos_user_resetting_request") }}>J'ai oublié mon mot de passe</a></div>
    <a class="btn btn-info" href={{ path("fos_user_registration_register") }}>S'inscrire</a>
</form>





